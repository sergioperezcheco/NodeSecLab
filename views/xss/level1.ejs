<%- include('../layout', { body: `
<div class="challenge">
  <div class="challenge-description">
    <h3>XSS - Level 1 (反射型XSS)</h3>
    <p>这个页面存在反射型XSS漏洞。尝试在输入框中注入JavaScript代码。</p>
    <p>提示：尝试使用 <code>&lt;script&gt;alert('XSS')&lt;/script&gt;</code> 作为输入。</p>
  </div>
  
  <div class="challenge-form">
    <form method="POST" action="/xss/level1">
      <div class="form-group">
        <label for="name">你的名字：</label>
        <input type="text" id="name" name="name" placeholder="输入你的名字">
      </div>
      <button type="submit" class="btn">提交</button>
      <a href="/source/xss/1" class="btn">查看源码</a>
    </form>
  </div>
  
  ${name ? `
    <div class="challenge-result">
      <h4>欢迎！</h4>
      <div class="message">
        你好，${name}！
      </div>
    </div>
  ` : ''}
</div>
` }) %> 